# mediaMTX binary (large, can be re-downloaded)
mediamtx
mediamtx_*.tar.gz

# Logs
*.log

# Cross-team handoff notes (Pi <-> Orin <-> app). Working correspondence, not
# project documentation; the durable content belongs in the architecture docs.
notes/

# Test videos
*.mp4

# Runtime files
*.pid
*.sock

# Python cache
__pycache__/
*.pyc

# Build output directories
build/
bin/

# Developer tooling: clangd's compile database and background index. Regenerate
# the database with: bear --output compile_commands.json -- make all
compile_commands.json
.cache/
